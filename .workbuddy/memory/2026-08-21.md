# 2026-08-21 工作日志

- 修改 `E:\WT\save-tokens\日志汇总\html报告\五场景汇总报告.html`：将5个场景的核心指标图从以0为基准线的正负变化率横向条形图，改为不带插件/带插件两组横向对比条形图（相对百分比）。因各指标单位不同，使用相对不带插件的百分比统一坐标，tooltip保留原始值与变化率。
- v2 视觉升级（用户反馈 UI 不好看）：重写整套 CSS 设计系统（背景点阵纹理、双层阴影、渐变顶部卡条、tabular-nums 数字、滚动条美化）；Hero 区重设计（玻璃统计卡、网格纹理、REPORT 水印）；表格升级（渐变表头、highlight 行左侧指示条）；全部 23 个 ECharts 图表统一圆角柱 + 灰色轴标签；侧边栏导航加指示条与渐变激活态。备份：`.bak`（原始）、`.bak-v2`（v2 前）。修复过一次正则替换丢失 `setOption(` 前缀的 bug，最终 JS 语法校验通过。
