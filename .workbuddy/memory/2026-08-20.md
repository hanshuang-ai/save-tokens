# 2026-08-20

## funlist 脚本审查与增强

- 复核 analyze-round-tokens.js / compare-round-tokens.js（用户第二版修改）：HTML div 38:38 平衡、CSS 闭合、total_all 补上 cache_creation 均已修复。残留低风险项：total_input/cache_hit_rate 分母仍缺 cache_creation（当前数据恒 0 不影响）；promptCumTotals 是累积值跨轮求和（非标准聚合）；promptData 与 avg* 字段为死代码。
- 应用户要求为 analyze-round-tokens.js 三个表格（逐轮 Token 明细 / 提示词构成累积量 / 新增量）各新增一个 Chart.js 堆叠柱状图：
  - 图表 A：新输入+缓存读取+缓存写入+输出（=总计列），仅 hasUsage 轮次，label 用 R+id（跳过错误轮）
  - 图表 B/C：6 类提示词成分堆叠（颜色与 compare 报告一致）
  - CDN 引入 chart.js@4.4.0，横向滚动容器 min-width=max(600, 轮数*22)
  - 验证：div 60:60 平衡、3 个 canvas 均初始化、内联脚本 node --check 通过、with/without-context-mode 场景均运行正常
- 措辞修正：用户指出"累积量"命名有误导——它实际是"当前轮请求 messages+system 的全量字符数"（当前轮全量），Claude Code 做 compaction 后会骤降，并非单调递增的历史累计。已将 analyze-round-tokens.js 所有用户可见文案从"累积量"改为"当前轮全量"，并在图表/表格描述中说明骤降原因；compare-round-tokens.js 中真正单调递增的"累积 Token 趋势线"保留原名（语义正确）。
- 用户反馈：不要删数据（显得不真实/作弊），改为保留全部轮次 + ✱ 标记 + 图下注释。标记条件定为 `!r.hasUsage`（比 isError 启发式更准，能覆盖 400 失败轮）。
- 全场景错误轮次分析结论（三类）：
  1. 标题生成 400：model=mimo-v2.5（Claude Code 后台小模型任务，system=1202 "Generate a concise title"，user 用 `<session>` 包裹），上游报 "model mimo-v2.5 not in allowed list"。每个用户子任务触发一次（场景4 有 5 个子任务→5 条）。主对话用 deepseek-v4-pro 正常。
  2. Explore 子代理 400：system≈3295 "You are a file search specialist"（READ-ONLY 探索子代理），同样 mimo-v2.5 被拒。
  3. count_tokens 404：上游 tokenhub 不实现该端点；user 内容=待预估的大段内容；404 后 SDK 会重试（without 场景1 出现同尺寸重复请求）。数量与上下文大小正相关：with 模式 16 条 vs without 模式 30 条（无插件上下文更大→压缩检查更频繁）。
- 环境坑：本机 Git Bash heredoc 会把 python 正则里的反斜杠吞掉（`\s`→`/s`），验证脚本需避免反斜杠转义或用字符串切分。
- 用户提醒：HTML 报告是脚本产物，脚本改动后所有报告都要重新生成，否则新旧报告口径不一致无法对比。已用新版 analyze-round-tokens.js 重跑 without-context-mode 场景1（83 轮，总 4,384,935 Token，命中率 87.5%），与 with-context-mode 版本措辞/图表完全一致。
- 用户追问 without-context-mode 场景1 的"当前轮全量"图为何犬牙交错：已拆解数据，确认三种原因叠加：
  1. 大段 tool_result 注入（红色）是尖刺主因，R60 单轮 tool_result 达 371,688 字符，R58 282,045，R56 189,033。
  2. Claude Code 上下文压缩/重置导致骤降：R60(386k) → R64(16k) 骤降约 37 万字符；R44(196k) → R45(30k) 骤降约 16.6 万字符。
  3. count_tokens 错误请求（✱）体里只有单条 user 消息，表现为孤立蓝色尖刺，其尺寸与被检查的目标 prompt 接近（R57/R59/R62/R63 约 18.8–19.4 万字符）。
- 压缩证据核实（用户问"这都是在日志里发现的吗"）：拆 request.jsonl 逐轮验证，三处骤降性质不同：
  1. R60→R64 是真 compaction：R64/R67/R69/R70/R71 的消息里逐字出现 "This session is being continued from a previous conversation that ran out of context"（R64 带 11,968 字符摘要 + "was read before the last conversation was summarized" 文件恢复注记）；且 R61-63 恰好是 count_tokens 上下文检查，因果链完整（386k → 连续 3 次 count_tokens → 压缩到 16k）。
  2. R44→R45 不是 compaction（无标记）：tool_result 块从 53→17、合计 166,553→18,782，最大 27.9k/25.5k/20.4k 的旧工具结果被丢弃——是 context-management beta 的 tool_result 清理/淘汰。
  3. R71→R72 也无压缩标记：R72 起消息数从 37 每轮稳定 +2，结构上是另一条对话链（线程切换/新任务），非压缩。局限：proxy 日志所有请求共用同一 x-claude-code-session-id，无独立 conversation id，主对话与子代理交错时只能靠消息结构推断。
- 关键日志字段备忘：Claude Code 压缩后的首轮 user 消息含两类标记文本——"This session is being continued from a previous conversation that ran out of context"（摘要本体）和 "<system-reminder> ... was read before the last conversation was summarized, but the contents are too large to include"（文件读取恢复注记），可直接 grep 定位所有压缩点。
- 全量扫描 10 个场景找压缩和子 Agent 证据（用户明确说 CLI 使用中触发了这些，要求从日志中找出）：
  - 压缩事件（RESET=messages.length骤降至1）：全 10 个场景共 37 次 RESET，8/10 场景有真压缩（仅 scenario-3 两个模式因条目太少未触发）。with-context-mode 共 16 次 RESET，without-context-mode 共 21 次。
  - 子 Agent 调用（name="Agent"）：4/10 场景有 Agent 工具调用，共 13 次（去重 by tool_use_id）：
    - with-context-mode/scenario-5: 8 次（1 Explore 失败 + 7 general-purpose，其中 6 次后台 agent）
    - without-context-mode/scenario-1: 3 次（全部 general-purpose 前台，返回 5K-6K 字分析报告）
    - without-context-mode/scenario-4: 1 次（Explore 失败 400）
    - without-context-mode/scenario-5: 1 次（Explore 失败 400）
  - 证据来源三处：request.jsonl 的 tool_use 块（含 subagent_type/description/prompt）+ response.jsonl 的 tool_calls 字段和 SSE stream 中的 content_block_start + request.jsonl 的 tool_result 块（子 agent 返回值）。
- 将压缩/Agent/系统切换/异常请求的检测方法固化到 funlist/detect-events.js：
  - 4 类检测：COMPACTION（4 信号取≥2 或 MARKER 单独确认）、SYS_ABBREVIATE/SYS_RESTORE（阈值 10000 字符）、AGENT_CALL（tool_use name=Agent，按 id 去重）、COUNT_TOKENS/HTTP_ERROR
  - 支持 `node detect-events.js --all --html` 一键扫描全部 10 场景，输出控制台汇总表 + HTML 报告
  - 全量扫描结果：426 请求 / 33 压缩 / 13 Agent / 43 系统切换 / 45 count_tokens / 21 错误
- analyze-round-tokens.js 新增图表 D（混合图）：逐轮提示词构成（当前轮全量，6 类堆叠柱状，左轴 y/字符）× 逐轮 Token 用量（折线，右轴 y1/Token）。用户选择：柱用当前轮全量视图、线用双线（总 Token 实线 #2d3436 + 总输入虚线 #576574 borderDash[6,3]）。X 轴与图表 B 对齐（含 ✱ 失败轮），失败轮折线置 null 断裂（spanGaps:false）；tooltip 按 dataset.type 区分 字符/Token 后缀。位置在图表 B 与图表 C 之间；文件头注释同步更新。验证：node --check 通过，with-context-mode/scenario-1（32 轮，94.7% 命中率）运行正常，HTML 中 spanGaps/borderDash/y1 配置均已确认。
- 用户截图质疑混合图"太服帖"：已核实非数据错误——柱是字符、线是 Token（如 R44 chars 193,572 vs token 94,125，R60 chars 390,647 vs token 181,677，chars/token≈1.5-2.2），折线正确绑定右轴 y1。视觉重合源于 Chart.js 双 Y 轴各自独立 auto-scale 到满高 + 两序列形状相似。已向用户提供 4 个修法选项（加注释/固定右轴比例=左轴/2.0/百分比归一化/放弃双轴），待用户确认。
- 用户要求全量重跑：10 个场景报告全部用新版 analyze-round-tokens.js 生成（proxy-logs 目录与 with/without scenario-4 时间戳相同，系同一份日志，未重复跑）。10 场景汇总：with 模式（1-5）32/31/15/31/68 轮、总 2.58M/2.97M/0.89M/3.10M/4.14M Token、命中率 94.7%/96.6%/90.4%/91.8%/88.9%；without 模式 83/35/7/23/35 轮、总 4.38M/2.02M/0.22M/1.98M/2.32M、命中率 87.5%/94.9%/81.3%/84.9%/87.9%。输出均为 funlist/round-tokens-test-scenarios-*.html。
- 用户质疑"总 Token"命名（逐轮 vs 累计歧义）：已核实数据正确——混合图折线「总 Token」=表格「总计」列（新输入+缓存读取+缓存写入+输出，单轮），顶部卡片「总 Token」才是会话累计；「总输入」= 新输入+缓存读取（模型实际看到的输入量，命中率分母）。已向用户解释并提议统一命名。
- 应用户要求将混合图折线改为「输入/输出」两类：输入（蓝 #0984E3 = 总输入 = 新输入+缓存读取）、输出（黄 #FDCB6E = output_tokens），删除原 总Token/总输入 双线，均为实线 borderWidth 2.5。同步更新文件头注释、图表说明文案、代码注释。验证：node --check 通过；with-scenario-1 R2 表格（总输入 34,281/输出 195）与折线数据一致；10 份报告已全部重跑，数字与改前一致。
- 混合图（图表 D）Y 轴去"字符数"：左轴标题 提示词字符数（当前轮全量）→ 提示词，右轴 Token 数 → Token；tooltip 柱状部分去掉" 字符"后缀（线仍标 Token）；图下说明文字同步改为（左轴，提示词）/（右轴，Token）。图表 D 容器高度 260px → 480px（内联 height 覆盖 .chart-scroll-inner），拉开逐轮数值差距。图表 B/C（单轴纯提示词图）与附录表格的"字符数"字样保留。验证：node --check 通过；with-scenario-1 HTML 中图表 D 块无"字符数"残留、提示词/Token 轴标题各 1 处、height:480px 已嵌入；10 份报告重跑，汇总数字与改前一致。
- 混合图（图表 D）Y 轴刻度改为 10k 一格：左右轴 ticks 均加 stepSize:10000 + beginAtZero:true，标签经 bigNumTick 显示 10K/20K…。先探测 10 场景两轴量级（提示词轴 max 60k~482k、Token 轴 max 35k~184k），10k 步进下 Token 轴 4~19 格、提示词轴最多 49 格（偏密，靠 autoSkip 跳标签，如用户嫌密可单独放宽提示词轴步进）。验证：stepSize:10000 在 with-scenario-1 HTML 图表 D 块出现 2 次（y+y1）；10 份报告重跑，汇总数字不变。
- 新增 funlist/build-comparison.js：解析 10 个 round-tokens HTML 的汇总卡片，按 test-scenarios 分类（with/without × 5 场景）生成开/关对照对比报告 context-mode-comparison.html。同时给两类矩阵（原始总量 + 每轮平均归一化）和缓存命中率聚合图。
- 关键发现：开/关两组并非受控 A/B——轮次与对话轨迹不同（如场景1：开 32 轮/关 83 轮；场景3：开 15/关 7），故"总 Token 节省率"被轮次扰动失真（4/5 场景显示开启更费，实因轮次更多）。唯一稳健信号是缓存命中率：开启组全场景 +1~+9 pts（平均 92.5% vs 87.3%），是 context-mode 预期收益。对比报告已加醒目"非受控实验"提示框。若需干净结论，应相同 prompt/轮次重跑。
