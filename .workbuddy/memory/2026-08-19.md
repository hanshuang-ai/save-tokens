# 2026-08-19

- 项目定位确认：`save-tokens` 工作区 = Claude Code API 流量抓包/审计工具链（`proxy.mjs` 本地拦截代理 → `proxy-logs/` JSONL → `analyze-proxy-log.js` → HTML 报告 `report.html` / `proxy-report-new/`）。proxy-logs 已有 2026-08-18 实测数据。
- `context-mode/` 是被分析的开源项目（MCP server，解决 AI agent 上下文膨胀）。关键 benchmark：376KB 原始 → 16.5KB 上下文（总节约 96%）；`ctx_execute_file`（结构化数据/日志/CSV）节约 95-100%；`ctx_index+ctx_search`（文档/代码检索）节约 50-93%，代码示例 100% 保真；>100KB 输出自动外部化到 FTS5 指针。
- 向用户解答了 context-mode 最大节约 token 的场景（日志/测试/构建输出/浏览器快照/CSV 分析等"只需统计结论"的大数据，配合 Think-in-Code 范式）。
- 为用户设计了「测出差异」的 A/B 测试场景矩阵（编码/非编码 × 0→1/1→N 四象限，共 12 个场景），并给出测量方法：同模型同任务两轮对照、proxy 抓累计 input/output token、ctx_stats 看索引量、每场景 3 次取中位数。预期 1→N 长会话类场景差异最大。
- 安装级别确认：context-mode 是按【项目】安装的 Claude Code 插件（installed_plugins.json 中 scope=project、projectPath=E:\WT\save-tokens，v1.0.169，2026-08-18 安装），仅在当前工作区生效；`context-mode/` 目录是源码 clone（版本同为 1.0.169）而非安装产物；其余插件（superpowers/frontend-design/claude-mem）为 user 全局 scope。
- 日志诊断（proxy-2026-08-19_02-08-33-592，10:16 结束）：该次 Claude Code 会话**未加载 context-mode 插件**——tools 只有 29 个内置工具、无任何 mcp__context-mode__* / ctx_* 工具、system 无插件注入；会话内仅有 Read/Grep 调用。插件本体健康（heal log=not-partial、缓存目录完整）。怀疑原因：会话运行目录非 E:\WT\save-tokens（project scope 不生效）或启动方式未加载 MCP。
- 第二次诊断（proxy-2026-08-19_04-10-15-723，12:15 结束，对应 WTP59_WisdomRefuel 项目会话）：插件**已注入但 0 次调用**。tools_available 含 `mcp__plugin_context-mode_context-mode__ctx_*`（ctx_execute/ctx_execute_file/ctx_index/ctx_search/ctx_fetch_and_index/ctx_batch_execute/ctx_purge/ctx_insight），system 有 routing 指令；实际调用仅 Read×57/Bash×6/Glob×6/Agent×1/Write×1，无 ctx_*。原因：WTP59 用 local scope 安装（settings.local.json 仅 enabledPlugins），**无 PreToolUse 路由 hook**（用户级 hooks 仅 SessionStart cache-heal），模型未受引导时默认用内置工具。另：代理不支持 /v1/messages/count_tokens（404），与插件无关。
- 第三次诊断（"带插件的日志"目录，proxy-2026-08-19_03-52-53-312，11:52-12:05）：同样**已注入 11 个 ctx_* 工具但 0 次调用**，实际仅 Bash×67。hooks 机制结论：插件官方 hooks/hooks.json 声明 6 类（PostToolUse/PreCompact/PreToolUse×多matcher/SessionStart/UserPromptSubmit/Stop），Claude Code 插件系统**自动注册无需手动配置**（新版已移除往 settings.json 写 hooks 的破坏性逻辑，#415）；但 PreToolUse 路由策略（core/routing.mjs）默认是**轻推放行（permit+nudge）而非强制拦截**——只对超大文件/危险命令才 modify/deny，所以 Read/Bash 全部放行、ctx_* 0 调用属设计内行为。想真正触发需主动提示模型用 ctx_*。
- 自研工具 vs CLI 差异（截图 2026-08-19 13:34）：用户自研 GUI 工具里 context-mode **真的被用了**——主动触发 `Skill: context-mode-instructions`、调用 `mcp__plugin_context-mode_context-mode__ctx_execute`（列目录）、`ctx_batch_execute`、同时启用 `mcp__plugin_claude-mem__mcp-search__search__smart_outline`。与 CLI 对比：CLI 0 次调用是 hook 策略软（permit+nudge）导致；自研工具**自动加载了 context-mode-instructions skill**（CLI 不自动触发），system prompt 注入更强/hook 策略更激进。CLI 版本是间接因素（2.1.223.970），更核心是客户端实现差异。
- 【源码确认·精化】自研工具 TinnoveCode（E:\WT\SDKCollection，Electron+Vue，基于 @anthropic-ai/claude-agent-sdk 0.3.152 + claude-code 2.1.152）为什么能用 context-mode。核心机制在 `node_modules/@tinnove/core/src/`：
  1) **插件加载**：claude-plugin-service.ts `resolveClaudeSdkPlugins()` → plugin-discovery.ts `listInstalledClaudePluginDirectories()` 读 `~/.claude/plugins/installed_plugins.json`（scope=user→全局、scope=local→按 projectPath 匹配当前目录）+ 项目 `.claude/plugins/` + UI 配置的 session 插件；`withClaudeControlQuery()`（claude-client.ts:590）创建 SDK query 时显式传 `plugins` + `settingSources:["project"]`。
  2) **skill 全文注入（与 CLI 的本质差异）**：plugin-discovery.ts `scanPluginSkills()`(L343) 扫描插件 `skills/*/SKILL.md`（缓存目录实测 8 个：context-mode/ctx-search/ctx-index/ctx-doctor/ctx-insight/ctx-purge/ctx-stats/ctx-upgrade）→ claude-plugin-service.ts `listSkillCatalog()` 聚合 → claude-client.ts `executeSkill()`(L2451)/`executeSkillById()`(L2482) 用 skill-discovery.ts `formatSkillContent()` 把 SKILL.md 全文包成 `<skill_content>` XML 块，**作为用户消息 sendMessage() 发出** → SKILL.md 里 "MANDATORY RULE: Default to context-mode for ALL commands" 直接进入模型上下文 → 模型遵守并调用 ctx_*。
  3) **触发入口**：服务端 chat.ts `/plugin-skill` 命令 → executeSkillById；前端管理面板（ManagePanel.vue）列出插件 skill（slashCommand 形如 /context-mode:context-mode）供点击。
  4) **CLI 为何 0 调用**：CLI 加载同一插件（MCP 工具注入 11 个 ctx_* 已被日志证实），但 SKILL.md 从不自动进入上下文（模型未主动调 Skill 工具），hooks 又是 permit+nudge 软策略 → 模型看不到强规则 → 0 次 ctx_* 调用。版本（SDK 2.1.152 vs CLI 2.1.223.970）是间接因素，非根因。
- 【版本变更 2026-08-19 14:06】用户决定停止根因分析，把本地 Claude Code CLI 从 2.1.223 降级到 2.1.152（与自研工具 TinnoveCode 内置 claude-code 版本一致）。执行方式：原生安装器 `claude install 2.1.152`（51s 完成）。现状：`claude --version` = 2.1.152；`~/.local/share/claude/versions/` 保留两个版本（2.1.152 新 / 2.1.223 旧）；`~/.local/bin/claude.exe` 已替换为 2.1.152（235MB），旧版自动备份为 `claude.exe.old.1787119602509`（280MB）。回滚：`claude install 2.1.223` 或改回 .old 文件。注意：CLI 是原生二进制（非 npm 包），不可用 npm i -g 覆盖；用户级配置 ~/.claude/settings.json（含 ANTHROPIC_BASE_URL=localhost:8080 代理、enabledPlugins）不受版本切换影响。
- 【斜杠命令机制 2026-08-19 14:48】context-mode 插件无 commands/ 目录，但 8 个 skill 中 7 个（ctx-doctor/ctx-index/ctx-insight/ctx-purge/ctx-search/ctx-stats/ctx-upgrade）frontmatter 声明了 `user-invocable: true` + `Trigger: /context-mode:ctx-xxx`，CLI 可直接斜杠触发（/ 菜单可见）。主 skill `context-mode`（含 MANDATORY RULE：大输出一律走 ctx_execute）**无 user-invocable**，只能靠模型自觉调用——这是 CLI 0 调用的另一面。可用方案：在 ~/.claude/commands/ 或项目 .claude/commands/ 自建斜杠命令注入 MANDATORY RULE 强制模型走 ctx_*。
